package org.firstinspires.ftc.teamcode.subsystems;

/*Notes:
* you can add more functionality this interface later on, but for now, we know every component on the robot has to have a function, which is what
* the abstract function() method is for.*/
public interface HardwareComponent {
    void function();
}
